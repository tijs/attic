/// AtticCore — shared library for backing up iCloud Photos to S3-compatible storage.
///
/// Used by both the Attic CLI and the Attic menu bar app.
public enum AtticCore {
    public static let version = "1.0.0-alpha.1"
}
